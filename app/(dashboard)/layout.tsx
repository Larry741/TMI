"use client";

import "@/styles/dashboard/globals.scss";
import "@/styles/dashboard/typography.globals.scss";
import "swiper/css";
import "swiper/css/grid";
import "swiper/css/pagination";

const Layout = ({ children }: { children: React.ReactNode }) => {
	return <>{children}</>;
};

export default Layout;
